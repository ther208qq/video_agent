# 虚拟环境
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 密钥 —— 绝对不能提交
.env
.env.local
.env.*.local
!.env.example

# 沙箱挂载的工作目录：容器里的运行时产物，不是源码
workspace/*

# 对话落盘：运行数据，不进版本库
.langgraph/

# 上游原始数据集副本：278 MiB，不参与构建，需要时可从上游重新获取。
# 注意 source/*.json（content_dataset / content_features / analysis_dataset）
# 是流水线的唯一真源，要进版本库，别一起忽略了。
source/metadata.parquet

# IDE
.idea/
.vscode/
